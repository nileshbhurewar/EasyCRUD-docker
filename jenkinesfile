pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning the repo '
            }
        }

        stage('Build') {
            steps {
                echo 'Building the project'
            }
        }

        stage('Run') {
            steps {
                echo 'Running the project'
            }
        }
    }
}
